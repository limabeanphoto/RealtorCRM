// components/stats/ModernMetricCard.js - Backward compatibility wrapper
// This file maintains backward compatibility while using the new unified component

import { ModernMetricCard } from './UnifiedMetricCard';

// Export the modern variant as the default ModernMetricCard
export default ModernMetricCard;